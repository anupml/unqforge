[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "unqforge"
version = "0.1.0"
description = "Generate iOS Shortcuts from Python, from evidence rather than guesswork"
readme = "README.md"
requires-python = ">=3.8"
license = { file = "LICENSE" }
keywords = ["ios", "shortcuts", "plist", "automation", "workflow"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent",
  "Topic :: Software Development :: Code Generators",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/anupml/unqforge"
Documentation = "https://github.com/anupml/unqforge/blob/main/docs/guide.md"

[tool.setuptools]
packages = ["unqforge"]

[tool.setuptools.package-data]
unqforge = ["constructs/*.json"]
